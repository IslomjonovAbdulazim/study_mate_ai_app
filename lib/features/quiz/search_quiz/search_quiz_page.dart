part of 'imports.dart';

class SearchQuizPage extends GetView<SearchQuizController> {
  const SearchQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
