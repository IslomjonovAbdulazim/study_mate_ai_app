part of 'imports.dart';

class VoiceToNotePage extends GetView<VoiceToNoteController> {
  const VoiceToNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
